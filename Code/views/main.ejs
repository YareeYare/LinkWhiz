<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body{
            font-family : 'Times New Roman', Times, serif;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Smol Links</title>
</head>
<body>
    <h1>URL Shortener</h1>
    <% if(locals.srtURL){ %>
        <p> Your short URL : localhost:4623/url/<%= srtURL %> </p>
    <% } %>

    <form action="/search" method="GET" align="center">
        <input type="text" name="query" placeholder="Enter your search query" />
        <button type="submit">Search</button>
    </form>
<br>
<hr>
<br>
    <div>
        <form method="POST" action="/url">
        <label>Enter your URL here!</label>
        <input
                type="text" name="url" placeholder="https://YourURL.com" 
        />

        <label>Note : </label>
        <input
                type="text" name="notes" placeholder="Add a note(optional)" 
        />

            <button type="submit"> Shrink it! </button>
        </form>
    <!-- </div>
    <div> -->
        <% if(locals.urls){%>
            <table border="0" height="50%" width="50%" align="center"
            style="border-spacing: 20px 0;">
                <thead>
                    <th>S. No.</th>
                    <th>Short URL</th>
                    <th>Original URL</th>
                    <th>Views</th>
                    <th>Notes</th>
                </thead>
                <tbody>
                    <% urls.forEach((link , i)=>{ %>
                        <tr>
                        <td><%= i+1 %></td>
                        <td><%= link.shortURL %></td>
                        <td><%= link.longURL %></td>
                        <td><%= link.views.length %></td>
                        <td><%= link.notes %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } %>
    </div>
</body>
</html>